# catalog-demo: yalnizca catalog-service yayinlayan yalın manifest
apiVersion: v1
kind: Namespace
metadata:
  name: catalog-demo
  labels:
    app.kubernetes.io/name: catalog-demo
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: catalog-config
  namespace: catalog-demo
data:
  ENVIRONMENT: "production"
  SERVICE_NAME: "catalog"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: catalog-sa
  namespace: catalog-demo
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: catalog-allow-namespace
  namespace: catalog-demo
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: catalog
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: catalog
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/name: catalog-demo
        # Kubelet/health probe'lar node IP'sinden geldiginden hazirlik kontrolleri
        # NetworkPolicy tarafindan engellenmesin diye tum kaynaklara izin veriyoruz.
        - ipBlock:
            cidr: 0.0.0.0/0
  egress:
    - {}
  policyTypes: ["Ingress", "Egress"]
---
apiVersion: v1
kind: Service
metadata:
  name: catalog
  namespace: catalog-demo
  labels:
    app.kubernetes.io/name: catalog
    app.kubernetes.io/part-of: catalog-demo
spec:
  selector:
    app.kubernetes.io/name: catalog
    app.kubernetes.io/part-of: catalog-demo
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog
  namespace: catalog-demo
  labels:
    app.kubernetes.io/name: catalog
    app.kubernetes.io/part-of: catalog-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: catalog
      app.kubernetes.io/part-of: catalog-demo
  revisionHistoryLimit: 3
  progressDeadlineSeconds: 120
  minReadySeconds: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: catalog
        app.kubernetes.io/part-of: catalog-demo
    spec:
      serviceAccountName: catalog-sa
      containers:
        - name: catalog
          image: docker.io/kursortak1996/catalog-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          env:
            - name: ASPNETCORE_URLS
              value: http://+:8083
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: catalog-config
                  key: ENVIRONMENT
            - name: SERVICE_NAME
              valueFrom:
                configMapKeyRef:
                  name: catalog-config
                  key: SERVICE_NAME
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /catalog/health
              port: 8083
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /catalog/health
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /catalog/health
              port: 8083
            failureThreshold: 12
            periodSeconds: 5
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: catalog-ingress
  namespace: catalog-demo
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: catalog-demo.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: catalog
                port:
                  number: 8083
